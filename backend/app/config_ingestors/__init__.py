"""Per-provider config ingestors.

Each ingestor pulls a resource-configuration snapshot from one cloud
provider and returns a list of `NormalizedResourceConfig`. This is the
Tier-2 data layer (asset/state inventory) from the plan doc.
"""

